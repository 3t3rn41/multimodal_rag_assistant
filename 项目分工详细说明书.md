# 基于多模态RAG的智能问答助手 —— 项目分工详细说明书

> 本说明书依据《项目分工简要说明书.txt》展开编写，对每个角色的工作内容进行详细铺开，明确各模块职责边界、交付物与协作接口，作为项目开发过程中的任务依据。

---

## 一、团队组成与协作总览

本项目采用三人制小团队，分工遵循"孙凡钧把门、刘喆消化、郑嘉辉思考"的原则：

| 角色 | 负责人 | 人数 | 定位 | 核心系统 |
| ---- | ------ | ---- | ---- | -------- |
| 全栈工程师 | 孙凡钧 | 1 人 | 骨干与门面 | 前端交互、后端服务、异步任务调度、基础设施 |
| 多模态数据工程师 | 刘喆 | 1 人 | 消化系统 | 数据解析、清洗、切片、特征提取、向量化入库 |
| AI 与 RAG 算法工程师 | 郑嘉辉 | 1 人 | 大脑 | 检索策略、重排、大模型问答、Prompt 工程 |

**协作原则：**

1. **数据契约先行**：刘喆产出的切片数据（含元数据字段）是郑嘉辉检索与孙凡钧展示溯源的基础，三者需在项目初期共同确定统一的 `Chunk` 数据格式（字段名、类型、时间戳格式等）。
2. **接口即边界**：各角色以明确定义的接口为协作边界，内部实现自由，接口变更需同步通知相关方。
3. **任务状态贯穿全链路**：从"上传 → 解析 → 向量化 → 检索 → 生成"的每个环节，由孙凡钧统一设计任务状态机，其余角色按状态节点交付能力。

---

## 二、孙凡钧：全栈工程师（1人）—— 项目的"骨干与门面"

### 2.1 总体定位

负责整个系统的**输入与输出**，掌控从用户上传文件到获得最终回答的全流程体验。确保用户交互顺畅、数据流转正常，是三人中唯一贯通"前端展示 → 后端服务 → 中间件基础设施 → 部署上线"全链路的角色。

### 2.2 职责一：前端交互开发

#### 2.2.1 知识库管理台

- **技术选型**：使用 React（推荐搭配 Vite + TypeScript）或 Vue3（搭配 Pinia + Vite），基于组件化开发，保持代码可维护性。
- **文件上传模块**：
  - 支持**拖拽上传**与点击选择两种方式，覆盖 PDF、Word、图片、音频、视频等常见格式。
  - 实现**分片上传 / 断点续传**（大视频文件动辄数百 MB，必须支持分片），上传过程中展示进度条与剩余时间。
  - 上传前进行文件类型、大小校验，非法文件给出明确提示。
- **解析进度展示**：
  - 通过轮询或 WebSocket 实时接收任务状态，以**步骤条/时间线**形式展示"解析中 → 向量化中 → 完成"三阶段。
  - 每个阶段展示具体子步骤与耗时（如"视频抽帧 23 张 / 共 120 张"），失败时展示错误原因并支持重试。
- **知识库列表与管理**：
  - 展示文件列表（名称、类型、大小、上传时间、解析状态、向量化状态）。
  - 支持文件的删除、重新解析、查看详情（该文件的 Chunk 数量、时间轴切片缩略预览等）。

#### 2.2.2 对话交互界面

- **流式打字机输出**：对接 SSE / WebSocket 流式接口，实现逐字输出的打字机效果，提升回答感知速度。
- **富文本渲染**：支持 Markdown 渲染（代码块、表格、列表），支持图片（检索到的图片描述或原图）与视频片段的内联展示。
- **引用溯源与点击播放**：
  - 回答中出现的引用标记（如 `[视频片段 01:20-01:45]`、`[图片 3]`）支持**点击跳转/弹窗预览**。
  - 视频引用点击后，调用前端播放器（如 video.js / 原生 video 标签）**精确跳转到 time_start 处播放**，展示时间轴高亮区间。
- **对话管理**：支持多会话切换、历史记录回看、清空会话、导出对话内容。

### 2.3 职责二：后端服务与 API

#### 2.3.1 后端框架

- **技术选型**：使用 **FastAPI**（Python 生态，与算法侧无缝衔接），利用其原生异步能力（async/await）支撑高并发流式输出。
- **项目结构**：采用分层架构（路由层 → 服务层 → 数据访问层），模块化拆分 `auth / upload / knowledge / chat / task` 等业务域。

#### 2.3.2 核心接口清单

| 模块 | 接口 | 说明 |
| ---- | ---- | ---- |
| 文件上传 | `POST /api/files/upload` | 接收分片上传，返回上传任务 ID |
| 知识库管理 | `GET/POST/DELETE /api/knowledge/*` | 文件列表、详情、删除、重新解析 |
| 任务状态 | `GET /api/tasks/{id}` / `WS /api/tasks/ws` | 轮询或 WebSocket 推送解析进度 |
| 对话 | `POST /api/chat` | 发起对话，支持 SSE 流式返回 |
| 历史 | `GET /api/chat/history` | 会话与消息历史查询 |
| 溯源 | `GET /api/chunks/{id}` | 返回 Chunk 的原始内容与元数据（供前端渲染引用） |

- **流式接口设计**：使用 **SSE（Server-Sent Events）** 作为首选（实现简单、自动重连、兼容性好），对话生成过程按事件流推送"增量文本 / 引用标记 / 图片 / 视频片段"等结构化事件；如后续需要双向通信（如任务进度主动推送）再引入 WebSocket。

### 2.4 职责三：异步任务调度（核心痛点）

#### 2.4.1 为什么必须异步

音视频处理动辄几分钟到十几分钟，若放在请求线程内同步执行，会导致接口超时、用户体验极差。因此必须将耗时任务放入后台队列异步执行。

#### 2.4.2 技术方案

- **消息队列与任务框架**：
  - 首选 **Celery + Redis**（生态成熟、支持定时任务与重试），Redis 同时承担消息代理与结果存储。
  - 若希望更轻量，可评估 **Dramatiq** 或基于 **arq**（纯 async）的方案。
- **任务队列划分**（按数据类型分队列，避免互相阻塞）：
  - `queue_document`：PDF / Word 文档解析队列。
  - `queue_media`：音频转写、视频抽帧等重计算队列。
  - `queue_vectorize`：向量化与入库队列。
- **并发与资源控制**：通过 Celery 的 worker 并发数与 `--concurrency` 控制 CPU 占用；视频任务设置超时与失败重试（如重试 2 次）。

#### 2.4.3 任务状态机

设计统一的任务状态机，覆盖所有异步任务：

```
PENDING（排队中）
   ↓  worker 领取
PROCESSING（处理中）
   ├── 子状态：PARSING（解析中）
   ├── 子状态：SLICING（切片中）
   ├── 子状态：EMBEDDING（向量化中）
   └── 子状态：INDEXING（入库中）
   ↓
SUCCESS（完成） / FAILED（失败，可重试）
```

- **状态存储**：任务状态写入关系型数据库（MySQL），Redis 仅做临时缓存与队列中转。
- **实时推送**：worker 每完成一个子阶段即更新任务状态，通过 WebSocket / SSE 推送给前端，前端据此渲染进度条。
- **失败处理**：记录失败原因、失败阶段与重试次数；超过重试上限标记为终态失败，前端提供"重新解析"入口。

### 2.5 职责四：数据与基础设施

#### 2.5.1 向量数据库

- **技术选型**：推荐 **Qdrant**（轻量、Docker 一键部署、支持过滤检索与 payload 存储，适合中小规模）或 **Milvus**（海量向量场景）。
- **职责**：负责向量库的部署、集合（Collection）设计（按 source_type 或知识库维度分集合）、索引参数调优（HNSW 的 M 与 ef_construct）、数据备份与恢复。

#### 2.5.2 关系型数据库设计（MySQL 5.7）

核心表设计：

| 表名 | 关键字段 | 用途 |
| ---- | -------- | ---- |
| `user` | id, username, password_hash | 用户账号（单人自用可简化） |
| `file_metadata` | id, file_name, file_type, file_size, storage_path, upload_time, status | 文件元数据管理 |
| `task` | id, file_id, task_type, status, sub_status, progress, error_msg, retry_count | 异步任务状态 |
| `chat_session` | id, user_id, title, created_time | 对话会话 |
| `chat_message` | id, session_id, role, content, refs, created_time | 消息记录（含引用 JSON） |
| `chunk_ref` | id, chunk_id, file_id, source_type, time_start, time_end | 溯源引用映射（与向量库 Chunk 对应） |

#### 2.5.3 对象存储（MinIO）

- **用途**：存储原始大文件（视频、音频、PDF）与解析过程生成的中间产物（抽帧图片、转写文本等）。
- **存储桶规划**：`raw-files`（原始文件）、`extracted-frames`（视频帧）、`transcripts`（转写文本）、`thumbnails`（封面图）。
- **职责**：MinIO 的部署、文件上传下载 SDK 封装、预签名 URL 生成（供前端直接播放/下载视频，避免后端吞吐瓶颈）、生命周期管理（过期文件清理）。

#### 2.5.4 Docker Compose 编排与一键部署

- 编排服务清单：`frontend`、`backend`、`mysql`、`redis`、`minio`、`qdrant`（或 milvus）、`celery-worker`、`celery-beat`（如需定时任务）。
- **部署脚本**：提供 `docker-compose up -d` 一键启动；编写 `.env` 环境变量模板（数据库密码、MinIO 密钥、模型 API Key 等）。
- **初始化流程**：编写启动脚本自动创建 MySQL 表结构（SQL 迁移脚本）与 MinIO 存储桶。
- **网络与安全**：服务间使用 Compose 内部网络隔离，仅暴露前端端口与必要端口。

### 2.6 孙凡钧的交付物清单

1. 前端源码（知识库管理台 + 对话界面）。
2. FastAPI 后端源码与接口文档（OpenAPI 自动生成）。
3. 异步任务队列模块（Celery 配置、任务定义、状态机实现）。
4. MySQL 建表 SQL 脚本。
5. Docker Compose 编排文件与部署文档。
6. 系统联调测试：打通"上传 → 解析 → 检索 → 流式回答 → 溯源播放"全链路。

---

## 三、刘喆：多模态数据工程师（1人）—— 项目的"消化系统"

### 3.1 总体定位

负责各类**非结构化数据**的解析、清洗、切片与特征提取，把"原始数据"转化为"可供检索的结构化 Chunk"。这是多模态 RAG **成败的关键**——上游解析质量直接决定下游检索与回答质量。

### 3.2 职责一：文档与图片解析

#### 3.2.1 复杂 PDF / Word 解析

- **技术选型**：使用 **PyMuPDF**（高精度文本与图片提取）、**Unstructured**（表格、版式解析）、配合 pdfplumber（精确表格坐标）等工具组合。
- **解析要点**：
  - 精准提取**正文文本**，过滤页眉页脚、页码等噪音。
  - 提取**表格**：保留行列结构，转换为 Markdown 表格或结构化 JSON，避免表格内容被横排打散。
  - 提取**嵌套图片**：定位图片在文档中的位置（页码 + 区域），连同上下文一并提取。
  - 处理扫描版 PDF：调用 OCR（如 PaddleOCR / Tesseract）兜底。

#### 3.2.2 图片描述生成

- **技术路线**：接入**视觉大模型（VLM）**（如 Qwen-VL、GPT-4o）或轻量模型 **BLIP** 生成图片描述。
- **策略**：
  - 重要/复杂图片走 VLM，生成**结构化描述**（主体、场景、文字内容、图表数据）。
  - 批量简单图片走 BLIP 等低成本模型，控制 API 成本。
  - 描述文本与图片原图（或缩略图）一起入库，检索时既可按文本描述命中，也可返回原图。

### 3.3 职责二：音频与视频处理（重头戏）

#### 3.3.1 音频处理

- **ASR 转写**：接入 **Whisper**（支持本地部署的 whisper.cpp / faster-whisper，或云端 API），将语音转为带时间戳的文本。
- **时间戳保留**：输出 `[start, end, text]` 形式的词级/句级时间戳序列，作为后续多模态对齐的基准。
- **说话人分离（可选增强）**：使用 pyannote 等工具区分说话人，生成对话式转写文本。

#### 3.3.2 视频处理

- **抽帧**：使用 **FFmpeg** 进行**关键帧提取**（按场景切换检测）或**均匀抽帧**（如每秒 1 帧），输出带时间戳的帧图片。
- **音轨提取**：FFmpeg 分离视频音轨（AAC/WAV），送入 ASR 流水线。
- **帧描述**：将抽取的帧图片送入图片描述管线，生成"画面描述 + 时间戳"。

#### 3.3.3 多模态对齐与融合（核心算法工作）

- **对齐原理**：将"画面帧描述"与"语音转写文本"基于**时间戳**对齐，两者时间轴相互映射。
- **融合策略**：
  - 同一时间窗口内既有语音又有画面变化时，融合为一条"图文同步"的切片。
  - 纯画面无语音时段（如演示操作、纯音乐段），以画面描述为主生成切片。
  - 形成**带时间定位的视频切片数据**，示例结构：

```json
{
  "file_id": "video_001",
  "source_type": "video",
  "time_start": 80,
  "time_end": 110,
  "content": "讲师演示如何配置环境变量，屏幕显示终端命令",
  "speech_text": "接下来我们配置环境变量，输入 export PATH=...",
  "frames": ["frame_001_80.jpg", "frame_001_85.jpg"],
  "keywords": ["环境变量", "export", "配置"]
}
```

### 3.4 职责三：数据切片与元数据构造

#### 3.4.1 多模态切分策略

| 数据类型 | 切分策略 |
| -------- | -------- |
| 文档 | 按**语义块**切分（标题层级 + 段落语义），辅助 LLM 语义分段或按 Markdown 结构切 |
| 长文本 | 按固定 token 窗口 + **重叠窗口**（如 512 token，重叠 64 token），避免切断语义 |
| 视频 | 每 **30 秒**为一个 Chunk，以语音转写为主体，融合画面描述 |
| 音频 | 按语义停顿（VAD）切分，或固定时长切分 |
| 图片 | 单张图片为一个 Chunk |

#### 3.4.2 Chunk 统一元数据格式（数据契约核心）

每个 Chunk 必须附加以下标准字段，供下游检索与溯源使用：

| 字段 | 说明 | 示例 |
| ---- | ---- | ---- |
| `chunk_id` | 全局唯一 ID | `c_8f3a...` |
| `file_id` | 所属文件 | `video_001` |
| `source_type` | 来源类型 | `document / image / audio / video` |
| `content` | 检索用的文本内容 | 融合后的文本 |
| `embedding` | 向量（由后续步骤生成） | `[0.12, -0.34, ...]` |
| `time_start` | 开始时间（媒体类） | `80` |
| `time_end` | 结束时间（媒体类） | `110` |
| `page` | 页码（文档类） | `12` |
| `media_path` | 原始媒体文件/帧路径 | `minio://.../frame_001_80.jpg` |
| `extra` | 扩展 JSON（关键词、标题层级等） | `{"keywords": [...]}` |

### 3.5 职责四：向量化入库

#### 3.5.1 Embedding 模型选型

- **调研目标**：对比 **CLIP**（图文对齐）、**jina-clip-v2**（多模态统一向量）、**BGE-M3**（中文语义、多粒度）等模型的检索效果与成本。
- **策略建议**：文本向量与图片向量使用**同一向量空间**（如 jina-clip-v2 / CLIP），确保"文字检索 → 图片命中"的跨模态检索成立；中文场景可配合 BGE 系列做文本增强。
- **向量维度与索引**：与向量库索引参数匹配（如 1024 维 → HNSW 参数调优）。

#### 3.5.2 批量写入

- 开发批量向量化脚本/服务，支持**断点续传**（已入库的 Chunk 跳过）。
- 写入向量库时**同步写入元数据字段**（payload），保证检索结果能携带完整溯源信息。
- 提供数据一致性校验：入库数量与源文件切片数量核对。

### 3.6 刘喆的交付物清单

1. 文档/图片/音频/视频解析流水线模块与文档。
2. 多模态对齐与融合模块。
3. 切分策略与 Chunk 元数据规范文档（数据契约）。
4. Embedding 模型选型对比报告与接入代码。
5. 向量化批量入库脚本。
6. 解析质量评估样例集（含各类典型文件）。

---

## 四、郑嘉辉：AI 与 RAG 算法工程师（1人）—— 项目的"大脑"

### 4.1 总体定位

负责**检索策略、重排、大模型问答生成与 Prompt 工程**，决定系统"答得准不准、答得好不好"。是最终用户体验的直接塑造者。

### 4.2 职责一：检索策略设计与优化

#### 4.2.1 混合检索

- **向量检索**：基于 Chunk embedding 的语义检索（依赖刘喆提供的向量库）。
- **关键词检索（BM25）**：实现或接入 BM25 检索（可基于 Qdrant 的全文检索能力，或独立引入 Elasticsearch / Meilisearch / Rank-BM25 库）。
- **混合融合策略**：实现 **RRF（Reciprocal Rank Fusion）** 或加权分数融合，将两类结果合并排序，**提升召回率**，避免单一检索模式的盲区。

#### 4.2.2 多模态检索决策

- 用户提问时，先进行**意图/模态判定**：
  - 纯文本意图 → 文本向量检索为主。
  - 涉及图片内容（"图中是什么""描述这个图表"）→ 结合**图片特征向量**检索（利用跨模态向量空间）。
  - 涉及视频（"视频里怎么讲的"）→ 检索视频切片（含时间戳的文本描述）。
- 实现 Query 改写/扩展：对短问题做查询扩展，提升命中率。

### 4.3 职责二：重排序

- **引入 Rerank 模型**（如 **bge-reranker** 系列，中文效果好），对混合检索召回的多个模态候选块（top-50 ~ top-100）进行**重新打分排序**。
- **噪音过滤**：设定分数阈值，过滤与问题无关或低相关的 Chunk。
- **多样性控制**：避免大量重复内容块占用上下文，按文件/来源做去重与多样性保留。
- **最终输入**：精排后保留 top-k（如 5~8 个）Chunk 送入生成阶段。

### 4.4 职责三：多模态上下文融合与生成

#### 4.4.1 多模态 Prompt 模板设计

- 设计统一的**组装规则**，将检索结果按模态组织进上下文：
  - 文本 Chunk → 原文段落。
  - 图片 Chunk → 图片描述文本 + 图片占位引用。
  - 视频 Chunk → 时间线摘要（`[00:20-00:50] 讲解内容：...`）。
- 示例模板结构：

```text
【系统提示】你是多模态知识库问答助手，基于提供的参考资料回答，引用必须标注来源。

【参考资料】
[文本块 1]（来自：文档A 第12页）...
[图片块 2]（来自：文档A 图片，描述：...）
[视频块 3]（来自：视频B 01:20-01:45）讲师演示...

【用户问题】...
```

#### 4.4.2 大模型接入

- **模型选型**：支持 **GPT-4o**、**智谱 GLM**、**通义千问**等（可配置切换），多模态能力强的模型可直接接收图片。
- **流式输出**：对接模型流式 API，与后端 SSE 打通，实现打字机效果。
- **超时与重试**：处理模型调用超时、限流，设置合理的失败降级策略。

#### 4.4.3 幻觉控制与引用溯源

- **强制引用**：Prompt 中明确要求"回答必须带引用来源"，引用格式如 `[视频片段 01:20-01:45]`、`[文档A 第12页]`、`[图片 3]`。
- **结构化引用输出**：要求模型在生成正文的同时输出结构化引用 JSON（引用块 ID 列表），后端据此渲染可点击的溯源链接。
- **无依据时拒答**：检索结果不足或无关时，引导模型明确说明"资料库中未找到相关内容"，避免编造。

### 4.5 职责五：效果评估与调优

#### 4.5.1 测试集构建

- 构建**小规模评估测试集**（如 30~50 个问题），覆盖文本、图片、视频、混合四类场景，每个问题标注标准答案与期望引用来源。

#### 4.5.2 评估指标

| 指标 | 说明 |
| ---- | ---- |
| 检索准确率（Precision） | 检索结果中相关 Chunk 占比 |
| 召回率（Recall） | 相关 Chunk 被检出的比例 |
| 命中率（Hit Rate） | 标准答案来源是否出现在 top-k 中 |
| 回答忠实度（Faithfulness） | 回答内容是否严格依据引用来源 |
| 引用准确率 | 模型给出的引用是否真实指向正确来源 |

#### 4.5.3 迭代优化

- 基于评估结果迭代优化：**切片策略**（与刘喆协作调整切片粒度）、**检索参数**（top-k、混合权重）、**Rerank 阈值**、**Prompt 措辞**。
- 建立回归测试流程：每次改动后跑一遍测试集，量化对比效果变化。

### 4.6 郑嘉辉的交付物清单

1. 混合检索模块（向量 + BM25 + RRF 融合）。
2. Rerank 模块（bge-reranker 接入与打分逻辑）。
3. 多模态 Prompt 模板库与组装逻辑。
4. 大模型接入封装（多厂商适配、流式输出）。
5. 引用溯源结构化输出模块。
6. 评估测试集与评估脚本、指标报告。

---

## 五、跨角色协作接口与里程碑建议

### 5.1 关键协作接口

| 接口/契约 | 提供方 | 消费方 | 说明 |
| --------- | ------ | ------ | ---- |
| Chunk 统一元数据格式 | 刘喆 | 孙凡钧、郑嘉辉 | 数据契约核心，最先冻结 |
| 任务状态机定义 | 孙凡钧 | 刘喆 | 刘喆按状态上报进度 |
| 向量库检索接口（API/客户端 SDK） | 孙凡钧（部署） | 郑嘉辉（调用） | 检索参数、返回结构约定 |
| 溯源数据查询接口 | 孙凡钧 | 前端 | 根据 chunk_id 取原始内容渲染 |
| 解析/切片中间产物路径约定 | 刘喆 | 孙凡钧 | MinIO 桶结构与命名规则 |
| Embedding 维度与索引参数 | 刘喆 | 孙凡钧 | 向量库集合创建依据 |

### 5.2 建议开发里程碑

1. **M1 契约冻结**：确定 Chunk 数据格式、任务状态机、向量库选型与集合设计。
2. **M2 最小闭环（端到端）**：上传一个 PDF → 解析切片 → 向量化 → 简单文本检索问答 → 前端展示，先跑通文本链路。
3. **M3 多模态能力补齐**：视频/音频解析、多模态对齐、图片检索上线。
4. **M4 质量优化**：混合检索 + Rerank + 评估迭代。
5. **M5 部署交付**：Docker Compose 一键部署，全链路验收。

---

*文档版本：v1.0　|　基于《项目分工简要说明书.txt》展开*
